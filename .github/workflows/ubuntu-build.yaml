name: ubuntu build
run-name: build runner on ubuntu 🚀

on:
  pull_request:
    branches:
      - main
  push:
    paths:
      - 'cmake/**'
      - 'include/**/*.h'
      - 'src/**/*.cc'
      - 'tests/**'
      - 'CMakeLists.txt'
      - 'scripts/**'
      - 'setup.py'
      - '.github/workflows/build.yaml'

jobs:
  build-amd-ubuntu:
    strategy:
      matrix:
        os: ["16.04", "18.04", "20.04", "22.04"]
    runs-on: ubuntu-latest
    container:
      image: docker://ubuntu:${{ matrix.os }}
      options: --volume ${{ github.workspace }}:/workspace/opendrive-cpp
    steps:
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: List files in the repository
        run: |
          ls /workspace/opendrive-cpp
      - name: Install dependence
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          apt update
          apt install -y python3-dev git pkg-config g++ cmake libtinyxml2-dev
      - name: Build
        run: |
          bash -c "cd /workspace/opendrive-cpp && python3 setup.py && source install/setup.bash && ./scripts/build.sh"
      - name: Cache
        uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.os }}
          path: |
            /workspace/opendrive-cpp/build/
            /workspace/opendrive-cpp/scripts/
            /workspace/opendrive-cpp/install/
            /workspace/opendrive-cpp/tests/data/

  test-amd-ubuntu:
    strategy:
      matrix:
        os: ["16.04", "18.04", "20.04", "22.04"]
    needs: build-amd-ubuntu
    runs-on: ubuntu-latest
    container:
      image: docker://ubuntu:20.04
      options: --volume ${{ github.workspace }}:/workspace/opendrive-cpp
    steps:
      - name: Cache
        uses: actions/download-artifact@v2
        with:
          name: ${{ matrix.os }}
      - name: Set file permissions
        run: chmod -R 755 /workspace/opendrive-cpp
      - name: Test
        run: |
          bash -c "cd /workspace/opendrive-cpp && source install/setup.bash && ./scripts/run_tests.sh"

